server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://49.235.106.52:3306/frients?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: Frients
    password: zKdR2ZGzNP5AWNXa
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      table-prefix: t_

sa-token:
  token-name: Authorization
  token-prefix: Bearer
  timeout: 604800
  active-timeout: -1
  is-concurrent: true
  token-style: uuid
  is-log: false

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: ethereal

socketio:
  host: 0.0.0.0
  port: 9093

langchain4j:
  open-ai:
    base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
    api-key: your-api-key-here
    model-name: qwen-plus

knife4j:
  enable: true
  setting:
    language: zh_cn
